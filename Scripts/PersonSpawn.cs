using UnityEngine;
using System.Collections;

public class PersonSpawn : MonoBehaviour {

	public float[] spawn_positions; 

	public int index;

	Transform transform;

	// Use this for initialization
	void Start () {
		//spawn_positions[0] = {150f, -1.54f, -272f};
		//spawn_positions[1] = {-86.6, -31.95, -707.36} };
		//index = Mathf.Random * 1;
		//transform = GetComponent<Transform>();
		//stransform.position(spawn_positions[index]);
	}
	
	// Update is called once per frame
	void Update () {
	}
}
